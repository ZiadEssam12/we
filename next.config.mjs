import withFlowbiteReact from "flowbite-react/plugin/nextjs";

/** @type {import('next').NextConfig} */
const nextConfig = {
  // No experimental settings needed for middleware
};

export default withFlowbiteReact(nextConfig);
